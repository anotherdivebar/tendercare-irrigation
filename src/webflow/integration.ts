/** Git-owned deployment configuration. Set an HTTPS endpoint only after implementing
 * the multipart POST contract in docs/WEBFLOW.md. Never put API keys in this file. */
export const webflowIntegration = { estimateEndpoint: "" };
