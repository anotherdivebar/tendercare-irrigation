# TODO: verified production origin, no trailing slash. Omit to keep preview noindex.
NEXT_PUBLIC_SITE_URL=
# Server-controlled form transport; set only after implementing and testing the endpoint.
NEXT_PUBLIC_ESTIMATE_ENDPOINT=
# Webflow CLI obtains workspace authentication interactively. Never commit credentials.
